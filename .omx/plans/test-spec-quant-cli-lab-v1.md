# Test Spec — Quant CLI Lab V1

- Unit-test redaction removes token/session/account-like fields.
- Unit-test quote history appends JSONL snapshots and computes changes.
- Unit-test classifier returns insufficient-data, momentum-candidate, mean-reversion-candidate, or watch.
- Unit-test subprocess wrapper builds tossctl commands safely.
- CLI smoke: doctor works without auth, help works, tests pass.
